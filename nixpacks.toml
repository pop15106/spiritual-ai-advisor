# Railway Nixpacks configuration
# Adds system dependencies required by Python packages (swisseph/Kerykeion needs sqlite3)

[phases.setup]
nixPkgs = ["sqlite"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[variables]
LD_LIBRARY_PATH = "/nix/var/nix/profiles/default/lib"
